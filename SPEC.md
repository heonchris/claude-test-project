# 냥집사 (가제) — 개인용 식단·물·운동 기록 앱 개발 명세서

> 이 문서는 이 앱의 기준 문서입니다. 앞으로의 모든 작업은 이 문서를 따릅니다.
> (원문 그대로 보관하고, 구현하면서 정해진 사항은 맨 아래 **11. 구현 메모**에 적어둡니다.)

---

## 1. 앱 개요

### 1-1. 한 줄 정의
> 사진 한 장과 탭 몇 번으로 오늘의 식단·물·운동을 남기면, 고양이가 반응해주는 **나 혼자 쓰는** 기록 앱.

### 1-2. 핵심 원칙 (반드시 지킬 것)
1. **로그인 없음, 서버 없음.** 모든 데이터는 폰 안에만 저장. 회원가입/계정 화면 만들지 말 것.
2. **기록은 3초 안에.** 사진 찍기 → 저장. 칼로리·영양소 입력은 전부 **선택 항목(optional)**.
3. **평가하지 않는다.** "목표 초과", "실패", 빨간 경고, 감점 표시 없음. 기록은 기록일 뿐.
   달성했을 때만 긍정 피드백을 주고, 못 채운 날은 조용히 넘어간다.
4. **인터넷 연결 없이 100% 동작.** AI 호출 코드 넣지 말 것 (플랜은 사용자가 붙여넣음).
5. 모든 UI 텍스트는 **한국어**.

### 1-3. 기록하는 것
- 🍚 **식단** — 사진 + 찍은 시각 + 끼니 구분 + 짧은 메모 (칼로리는 선택)
- 💧 **물** — 컵 단위 탭 (기본 목표 8컵, 설정에서 변경)
- 🏃 **운동** — 종류 + 시간(분) + 메모
- 📋 **플랜 체크** — 붙여넣은 목표/식단표의 오늘 항목 체크

---

## 2. 기술 스택

```
Expo SDK (최신)
expo-router          — 파일 기반 화면 라우팅 (탭 네비게이션)
expo-sqlite          — 로컬 DB
expo-image-picker    — 카메라 촬영 / 갤러리 선택
expo-image-manipulator — 사진 리사이즈(가로 1080px) 후 저장, 용량 절약
expo-file-system     — 사진 파일 앱 내부 폴더에 영구 보관
react-native-svg     — 고양이 캐릭터 (이미지 파일 대신 코드로 그림)
date-fns             — 날짜 처리
```

- 상태관리 라이브러리 쓰지 말 것. React 기본 훅 + SQLite 조회로 충분.
- TypeScript 사용.
- 사진은 DB에 넣지 말고 **파일로 저장하고 경로(uri)만 DB에 기록**.

---

## 3. 데이터 모델 (SQLite)

```sql
-- 식단 기록
CREATE TABLE meals (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT NOT NULL,           -- 'YYYY-MM-DD'
  meal_type TEXT NOT NULL,      -- '아침' | '점심' | '저녁' | '간식'
  photo_uri TEXT,               -- 없을 수도 있음
  memo TEXT,
  calories INTEGER,             -- 선택. NULL 허용
  taken_at TEXT,                -- 사진을 찍은 시각(EXIF). 없으면 저장한 시각
  created_at TEXT NOT NULL
);

-- 물 (하루 한 줄)
CREATE TABLE water (
  date TEXT PRIMARY KEY,
  cups INTEGER NOT NULL DEFAULT 0
);

-- 운동
CREATE TABLE workouts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  date TEXT NOT NULL,
  name TEXT NOT NULL,           -- '걷기', '헬스', '요가' 등
  minutes INTEGER,
  memo TEXT,
  created_at TEXT NOT NULL
);

-- 붙여넣은 플랜 (여러 개 저장 가능, 하나만 active)
CREATE TABLE plans (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  start_date TEXT NOT NULL,
  end_date TEXT,
  raw_text TEXT NOT NULL,       -- 붙여넣은 원문 (항상 보관)
  parsed_json TEXT,             -- 구조화 성공 시 JSON, 실패 시 NULL
  is_active INTEGER DEFAULT 0,
  created_at TEXT NOT NULL
);

-- 플랜 항목 체크
CREATE TABLE plan_checks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  plan_id INTEGER NOT NULL,
  date TEXT NOT NULL,
  item_key TEXT NOT NULL,       -- 예: 'meal-아침', 'workout-0'
  label TEXT NOT NULL,
  done INTEGER DEFAULT 0,
  UNIQUE(plan_id, date, item_key)
);

-- 설정 (key-value)
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT
);
-- 기본값: water_goal=8, cat_name='나비', reminder_on=0
```

---

## 4. 화면 명세

하단 탭 4개: **오늘 · 기록 · 플랜 · 설정**

### 4-1. 오늘 (홈) — `app/(tabs)/index.tsx`
위에서부터:
1. **고양이 카드** (화면의 주인공, 높이 화면의 35% 정도)
   - 가운데 큰 SVG 고양이. 아래 6번 규칙대로 표정/소품이 바뀜.
   - 고양이 아래 한 줄 말풍선 대사 (아래 6-3 참고)
2. **오늘의 링 3개** — 식단 / 물 / 운동을 원형 진행바로 나란히
3. **물 컵 줄** — 컵 아이콘 8개(설정값만큼). 탭하면 채워짐, 길게 누르면 하나 취소
4. **오늘 먹은 것** — 사진 썸네일 가로 스크롤. 없으면 "아직 비어 있어요. 첫 끼를 남겨볼까요?" + 추가 버튼
5. **오늘 운동** — 한 줄 요약 (예: `걷기 40분`), 없으면 추가 버튼
6. **오늘의 플랜** — 활성 플랜의 오늘 항목 체크리스트. 플랜 없으면 이 섹션 자체를 숨김
7. 우하단 **플로팅 + 버튼** → 기록 추가 시트

### 4-2. 기록 추가 (모달 시트)
`+` 누르면 세 갈래 선택: **식단 / 운동 / 물**

**식단 추가**
- 상단에 큰 버튼 2개: `사진 찍기` / `앨범에서 고르기`
- 사진 고르면 미리보기 → 끼니 선택(아침/점심/저녁/간식, 현재 시각으로 자동 추천) → 메모(선택) → 칼로리(선택, 접혀 있음) → `저장`
- **사진 없이도 저장 가능해야 함** (메모만으로도 OK)

**운동 추가**
- 종류: 자주 쓰는 것 칩으로 (걷기·달리기·헬스·요가·홈트·자전거·기타) + 직접 입력
- 시간(분): 숫자 입력 + `+10분` 퀵버튼
- 메모(선택) → `저장`

### 4-3. 기록 (달력/통계) — `app/(tabs)/history.tsx`
- 상단: 월 달력. 날짜 칸 아래 작은 점 3개(식단🟠/물🔵/운동🟢) — 그날 기록 있으면 켜짐
- 날짜 탭 → 하단에 그날 상세 (사진 그리드, 물 컵 수, 운동 목록). 여기서 삭제·수정 가능
- 상단 토글로 **주간 요약** 전환: 최근 7일 물/운동 막대그래프, 식단 사진 모아보기

### 4-4. 플랜 — `app/(tabs)/plan.tsx`
- 활성 플랜 없으면: 큰 붙여넣기 영역 + `붙여넣은 내용 불러오기` 버튼 + 아래 5번의 **프롬프트 복사 버튼**
- 붙여넣기 처리 로직:
  1. 텍스트가 **JSON으로 파싱되면** → 구조화 저장 → 날짜별 체크리스트 자동 생성
  2. 파싱 실패하면 → **원문 그대로 저장**하고, 줄 단위로 잘라서 체크 가능한 리스트로 보여줌 (실패했다고 거절하지 말 것)
- 활성 플랜 있으면: 목표 제목 / 진행 일차(예: `3일차 / 28일`) / 오늘 항목 체크리스트 / 전체 보기(펼침) / `플랜 교체` 버튼
- 지난 플랜 목록도 아래에 보관

### 4-5. 설정 — `app/(tabs)/settings.tsx`
- 고양이 이름 바꾸기
- 하루 물 목표 컵 수 (4~15)
- 알림: 식사/물 리마인더 on/off + 시간 (expo-notifications, 로컬 알림만)
- **데이터 내보내기** — 전체 기록을 JSON 파일로 저장/공유 (expo-sharing). 백업 수단이 이거뿐이라 꼭 필요
- 데이터 불러오기 (내보낸 JSON 복원)
- 저장 공간: 사진 개수 / 용량 표시, 오래된 사진 정리

---

## 5. 플랜 붙여넣기 규격 ★핵심

AI가 만들어준 목표·식단을 앱이 알아듣게 하려면 **형식을 정해두는 게 전부**입니다.
설정 화면과 플랜 화면에 아래 프롬프트를 **복사하는 버튼**이 있습니다.
사용자는 이걸 복사 → 챗봇에 붙여넣기 → 나온 JSON을 다시 앱에 붙여넣기 하면 끝.

### 5-1. 앱이 읽는 JSON 형식

```json
{
  "title": "4주 체력 만들기",
  "startDate": "2026-08-25",
  "endDate": "2026-09-21",
  "dailyTargets": { "waterCups": 8, "workoutMinutes": 30 },
  "days": [
    {
      "day": 1,
      "meals": [
        { "type": "아침", "name": "오트밀과 바나나", "items": ["오트밀 40g", "바나나 1개", "우유 200ml"] },
        { "type": "점심", "name": "닭가슴살 샐러드", "items": [] },
        { "type": "저녁", "name": "된장찌개 정식", "items": [] }
      ],
      "workouts": [
        { "name": "빠르게 걷기", "minutes": 30, "detail": "저녁 식후" }
      ],
      "note": "물 자주 마시기"
    }
  ]
}
```

- `days`가 7개만 오면 → **7일 주기로 반복** 적용 (28일 플랜이어도 4주 반복)
- 없는 필드는 전부 무시하고 있는 것만 쓸 것. 필드 하나 빠졌다고 통째로 실패시키지 말 것.

### 5-2. "프롬프트 복사" 버튼이 복사하는 텍스트

원문은 `lib/promptText.ts`에 있습니다.

```
아래 조건으로 식단·운동 계획을 짜줘.
결과는 반드시 아래 JSON 형식으로만 출력하고, 설명이나 코드블록 표시 없이 JSON만 줘.

[내 조건: 여기에 나이/키/몸무게/목표/알레르기/운동 가능 시간 등을 적으세요]

{
  "title": "계획 이름",
  "startDate": "YYYY-MM-DD",
  "endDate": "YYYY-MM-DD",
  "dailyTargets": { "waterCups": 숫자, "workoutMinutes": 숫자 },
  "days": [
    {
      "day": 1,
      "meals": [{ "type": "아침", "name": "메뉴명", "items": ["재료/분량"] }],
      "workouts": [{ "name": "운동명", "minutes": 숫자, "detail": "설명" }],
      "note": "그날의 한 줄 팁"
    }
  ]
}
```

---

## 6. 고양이 캐릭터 규칙

### 6-0. 스타일 ★필수
참고 이미지는 `assets/cat-reference.jpg`에 둡니다. (없어도 앱은 동작합니다. 아래 규칙이 실제 기준입니다.)

**이 스타일을 지킬 것:**
- **완전한 검정 실루엣.** 외곽선·그림자·그라데이션 없이 **검정 면 하나**로만
- **눈만 흰색.** 큰 흰 동그라미 안에 검은 동공. 이게 이 캐릭터의 전부
- 귀는 **뾰족한 삼각형**, 머리에서 바로 솟은 형태
- 몸통은 길쭉하고 홀쭉함 (통통한 고양이 아님)
- 꼬리는 **가늘고 길게**, 곡선으로
- 발끝에 **세 갈래 발가락**이 보이게
- 매달릴 때 발 위로 **가느다란 직선 3~4개 = 발톱 자국**
- 입·코·수염·볼터치 **넣지 말 것** (눈만 있어야 이 느낌이 삽니다)

### 6-1. 그리는 법
`react-native-svg`로 **코드로 그림** (그림파일 안 씀 → 말로 수정 가능).
`components/Cat.tsx` 한 파일에서 props를 받아 렌더링.
몸통은 고정하고 **눈동자 위치·크기, 팔다리 각도, 꼬리 곡선**만 바꿔서 상태를 표현합니다.

| state | 조건 | 모습 |
|---|---|---|
| `napping` | 오늘 기록 0개 (기본값) | 바닥에 늘어져 누움, 눈 감음(가로선), 꼬리 축 처짐 |
| `awake` | 기록 1개 이상 | 앉아서 정면 응시, 눈 크게 |
| `climbing` | 기록 2종 이상 | 참고 이미지 그대로 매달린 자세, 발톱 자국 |
| `top` | 3종 다 + 물 목표 달성 | 꼭대기에 올라앉아 꼬리 살랑 |
| `startled` | 저장 직후 1.5초 | 눈동자 확 커지고 몸이 살짝 튐 |

### 6-2. 시그니처 — "오늘의 벽 오르기"
고양이 카드는 **세로로 긴 흰 벽**입니다. 고양이는 오늘 달성률만큼 그 벽을 **위로 올라갑니다.**

- 높이 = (물 + 식단 + 운동 + 플랜체크) 달성률 평균
- 올라간 자리마다 **발톱 자국이 흔적으로 남음** → 오늘 얼마나 했는지가 한눈에
- 벽 옆에 카테고리 색 점 3개(살구·하늘·민트)로 뭘 했는지 표시
- 올라갈 때 부드럽게 스르륵 이동(스프링 애니메이션), 자정에 리셋

> **중요:** 바닥에 있는 상태는 "실패"가 아니라 **낮잠 자는 귀여운 상태**로 보이게 할 것.
> 바닥에 X표시, 빨간색, 흐린 처리 같은 벌칙 연출 금지.

**이게 이 앱의 유일한 화려한 부분**이니 나머지 화면은 조용하고 단정하게 유지할 것.

### 6-3. 말풍선 대사 (한 줄, 랜덤)
- 기록 없음: `자는 중…` / `깨우려면 뭐 하나 남겨봐요`
- 기록 중: `올라가는 중` / `잘하고 있어요`
- 물 달성: `물 다 마셨다냥`
- 전부 달성: `꼭대기 도착. 오늘 푹 쉬어요`

> 잔소리·재촉·죄책감 주는 대사는 절대 넣지 말 것. 못 채운 날엔 그냥 자고 있으면 됩니다.

---

## 7. 디자인 토큰

```ts
export const colors = {
  bg:        '#FBF7F0',  // 우유빛 배경
  card:      '#FFFFFF',
  text:      '#3D3733',
  textSub:   '#9A9088',
  meal:      '#F5B461',  // 식단 - 살구
  water:     '#7FB6E8',  // 물 - 하늘
  workout:   '#7BC6A4',  // 운동 - 민트
  catBody:   '#141312',  // 고양이 - 완전 검정에 가깝게
  catEye:    '#FFFFFF',
  wall:      '#FFFFFF',  // 고양이가 오르는 벽
  line:      '#EFE7DC',
};
```

> 고양이가 검정이라 **화면에서 가장 진한 것은 고양이 하나뿐**이어야 합니다.
> 다른 곳에 검정에 가까운 색을 쓰지 마세요. 아이콘·글씨는 `text`(#3D3733)까지만.

- 카테고리 색 3개는 **장식이 아니라 정보**입니다. 링·달력 점·기록 카드 테두리에서 항상 같은 색을 유지하세요.
- 폰트: 본문 **Pretendard**, 제목/숫자 **Cafe24 Ssurround** (둘 다 무료, `expo-font`로 로드)
- 모서리 radius: 카드 20, 버튼 16, 칩 999
- 그림자는 아주 옅게 (`shadowOpacity 0.05`). 진한 그림자 금지.
- 여백 단위 4의 배수. 화면 좌우 패딩 20.

---

## 9. 완성 후 점검 리스트

- [ ] 비행기 모드에서 모든 기능이 동작한다
- [ ] 사진 30장쯤 넣어도 앱이 느려지지 않는다
- [ ] 앱을 껐다 켜도 데이터가 남아 있다
- [ ] 자정이 지나면 오늘 화면이 새 날짜로 초기화된다
- [ ] 기록을 안 한 날에 앱이 나를 탓하지 않는다
- [ ] 설정에서 내보낸 JSON으로 복원이 된다

---

## 10. 나중에 붙이면 좋을 것 (지금은 만들지 말 것)

- 사진에서 음식 자동 인식 (Claude API 연동)
- 체중 기록 + 그래프
- 고양이 성장/코스튬
- iOS 위젯

---

## 11. 구현 메모 (만들면서 정해진 것들)

명세와 다르게 간 곳, 명세에 없어서 정한 곳만 적습니다.

**고양이 컴포넌트가 두 개로 나뉘어 있음**
- `components/Cat.tsx` — 고양이 그 자체. props는 `state`, `width`
- `components/CatWall.tsx` — 벽 + 오르기(`progress`) + 발톱 자국 + 말풍선 + 카테고리 점
- 명세 6-1의 `progress`는 벽이 갖고 있는 값이라 `CatWall`로 옮겼습니다.

**달성률 계산** (`lib/progress.ts`)
- 식단 = 오늘 기록한 끼니 수 / 3
- 물 = 컵 / `water_goal`
- 운동 = 합계 분 / `workout_goal_minutes` (기본 30, 시간을 안 적으면 100%로 봄)
- 플랜 = 오늘 체크한 항목 / 오늘 항목 수 (플랜이 없으면 평균에서 제외)
- 벽 높이 = 위 값들의 평균

**플랜의 `dailyTargets`는 설정에 반영됩니다**
- 플랜을 시작할 때 `waterCups`(4~15)와 `workoutMinutes`가 있으면 설정값으로 저장합니다.
- 그래야 설정 화면과 오늘 화면이 같은 목표를 봅니다.

**폰트**
- `assets/fonts/`에 Pretendard(본문)와 Cafe24 Ssurround(제목/숫자)를 넣어 뒀습니다.
- 폰트 로딩이 실패해도 시스템 폰트로 조용히 넘어갑니다(앱이 죽지 않게).
- 모든 글씨는 `components/Txt.tsx`를 거칩니다. 크기/굵기를 바꾸려면 `theme/typography.ts`만 보면 됩니다.

**아이콘 / 스플래시**
- `tools/render_assets.py`가 고양이 도형을 그대로 PNG로 뽑습니다 (`python3 tools/render_assets.py`).
- `tools/cat-preview.png`는 5가지 상태를 한 장에 모아 놓은 미리보기입니다. 앱을 켜지 않고도 모양을 확인할 수 있습니다.
- 이 스크립트는 **미리보기·아이콘 전용 복사본**입니다. 모양의 원본은 언제나 `components/Cat.tsx`입니다.

**식단의 시각 (`taken_at`)**
- 사진을 고르면 EXIF에서 촬영 시각을 읽어 `taken_at`에 넣습니다. 정보가 없으면 저장한 시각을 씁니다.
- 끼니 자동 추천도 '지금'이 아니라 **사진을 찍은 시각** 기준입니다.
  (아침에 찍어둔 사진을 밤에 저장해도 '아침'으로 잡힙니다)
- 사진이 오늘 것이 아니면 "그날 기록으로 남길까요?" 스위치가 뜹니다. 기본값은 켜짐입니다.
- 오늘 화면과 기록 화면의 사진은 `taken_at` 순서로 늘어섭니다.
- 기존 폰의 DB는 `db/index.ts`의 `migrate()`가 열을 추가하고, 옛 기록은 저장 시각으로 채웁니다.

**사진 백업**
- 내보내기 JSON에는 사진 파일이 들어가지 않습니다(용량 때문). 경로만 들어갑니다.
- 설정 화면에도 이 점을 적어 두었습니다.

**앱 화면 구성 파일**
```
app/_layout.tsx          앱 시작 (DB 초기화, 폰트 로딩)
app/(tabs)/index.tsx     오늘
app/(tabs)/history.tsx   기록
app/(tabs)/plan.tsx      플랜
app/(tabs)/settings.tsx  설정
app/add/meal.tsx         식단 추가/수정 (모달)
app/add/workout.tsx      운동 추가/수정 (모달)
```
